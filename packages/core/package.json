{
  "name": "@import-validator/core",
  "version": "0.1.0",
  "private": true,
  "description": "WASM loader, Engine API, and shared helpers for ImportValidator",
  "license": "SEE LICENSE IN LICENSE",
  "type": "module",
  "files": [
    "dist"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./wasm": {
      "import": "./dist/wasm/index.js",
      "types": "./dist/wasm/index.d.ts"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build:wasm": "node scripts/build-wasm.mjs",
    "build:wasm:fast": "node scripts/build-wasm.mjs",
    "build:wasm:full": "WASM_FEATURES=pattern node scripts/build-wasm.mjs",
    "build:pkg": "rimraf dist && tsc -p tsconfig.json && node scripts/copy-wasm.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "pnpm run build:wasm && rimraf dist && tsc -p tsconfig.json && node scripts/copy-wasm.mjs"
  }
}
